#include <stdio.h>

int main()
{
    int a, b, c,mid;
    scanf("%d%d%d",&a,&b,&c);
    if (a < b)
    {
        mid = a;
        a = b;
        b = mid;
    }
    if (a < c)
    {
        mid = a;
        a = c;
        c = mid;
    }
    printf("%d", a);
    return 0;
}
